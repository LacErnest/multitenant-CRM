# Config file for test1.sql
secret = 'lapin'
stats  = 'no'
tables = {
   `addresses` = {
     `addressline_1`                     = texthash 10
     `addressline_2`                     = texthash 10
     `city`                              = texthash 10
     `region`                            = texthash 10
     `postal_code`                       = texthash 10
   }
   `comments` = {
     `content`                           = texthash 15
   }
   `companies` = {
     `name`                            = texthash 5
   } 
   `company_loans` = {
     `amount`                            = inthash 5
     `description`                       = texthash 30
     `admin_amount`                      = inthash 5
     `amount_left`                       = inthash 4
     `admin_amount_left`                 = inthash 4
   }
   `company_rents` = {
     `amount`                            = inthash 5
     `name`                              = texthash 10
     `admin_amount`                      = inthash 5
   }
   `credit_notes` = {
     `reference`                         = texthash 5
     `total_price`                       = inthash 5
     `total_vat`                         = inthash 4
     `total_price_usd`                   = inthash 5
     `total_vat_usd`                     = inthash 4
   }
   `employee_histories` = {
     `salary`                            = inthash 5
     `salary_usd`                        = inthash 5
     `employee_salary`                   = inthash 5
     `working_hours`                     = inthash 2
   }
   `employees` = {
     `first_name`                        = texthash 5
     `last_name`                         = texthash 5
     `email`                             = emailhash 'magicmedia.studio' 10
     `salary`                            = inthash 5
     `working_hours`                     = inthash 2
     `phone_number`                      = inthash 6
     `linked_in_profile`                 = fixed ''
     `facebook_profile`                  = fixed ''
   }
   `invoice_payments` = {
     `pay_amount`                        = inthash 5
   }
   `invoices` = {
     `total_price`                       = inthash 5
     `total_vat`                         = inthash 4
     `total_price_usd`                   = inthash 5
     `total_vat_usd`                     = inthash 4
     `manual_price`                      = inthash 5
     `manual_vat`                        = inthash 4
     `down_payment`                      = inthash 5
   }
   `items` = {
     `service_name`                        = texthash 10
     `description`                         = texthash 30
     `quantity`                            = fixed unquoted '1'
     `unit_price`                          = inthash 5
   }
   `loan_payment_logs` = {
     `amount`                            = inthash 5
     `admin_amount`                      = inthash 5
   }
   `orders` = {
     `total_price`                       = inthash 5
     `total_vat`                         = inthash 4
     `total_price_usd`                   = inthash 5
     `total_vat_usd`                     = inthash 4
     `manual_price`                      = inthash 5
     `manual_vat`                        = inthash 4
   }
   `projects` = {
     `name`                              = texthash 10
     `budget`                            = inthash 5
     `budget_usd`                        = inthash 5
   }
   `purchase_orders` = {
     `total_price`                       = inthash 5
     `total_vat`                         = inthash 4
     `total_price_usd`                   = inthash 5
     `total_vat_usd`                     = inthash 4
     `manual_price`                      = inthash 5
     `manual_vat`                        = inthash 4
     `reason_of_rejection`               = fixed ''
     `reason_of_penalty`                 = fixed ''
     `reason`                            = fixed ''
   }
   `quotes` = {
     `total_price`                       = inthash 5
     `total_vat`                         = inthash 4
     `total_price_usd`                   = inthash 5
     `total_vat_usd`                     = inthash 4
     `manual_price`                      = inthash 5
     `manual_vat`                        = inthash 4
     `down_payment`                      = inthash 5
   }
   `resources` = {
     `name`                              = texthash 10
     `first_name`                        = texthash 5
     `last_name`                         = texthash 5
     `email`                             = emailhash 'magicmedia.studio' 10
     `phone_number`                      = inthash 6
     `tax_number`                        = fixed ''
     `job_title`                         = texthash 5
   }
   `services` = {
     `name`                              = texthash 10
     `price`                             = inthash 4
     `price_unit`                        = texthash 3
   }
   `banks` = {
     `name`                              = texthash 10
     `iban`                              = texthash 6
     `bic`                               = texthash 6
     `account_number`                    = fixed '0000000000'
     `routing_number`                    = fixed '0000000000'
     `usa_account_number`                = fixed '0000000000'
     `usa_routing_number`                = fixed '0000000000'
   }
   `checks` = {
     `last_run_message`                  = texthash 20
   }
   `commission_payment_logs` = {
     `amount`                            = inthash 5
   }
   `commissions` = {
     `total`                             = inthash 5
     `paid_value`                        = inthash 5
   }
   `company_notification_settings` = {
     `cc_addresses`                      = fixed unquoted '["email@magicmedia.studio"]'
     `invoice_submitted_body`            = fixed '<p>Invoice has been submitted</p>'
     `from_address`                      = emailhash 'magicmedia.studio' 10
     `from_name`                         = texthash 10
   }
   `contacts` = {
     `first_name`                        = texthash 5
     `last_name`                         = texthash 5
     `email`                             = emailhash 'magicmedia.studio' 10
     `phone_number`                      = inthash 6
     `department`                        = texthash 5
     `title`                             = texthash 5
     `linked_in_profile`                 = fixed ''
   }
   `customer_addresses` = {
     `addressline_1`                    = texthash 10
     `addressline_2`                    = texthash 10
     `city`                              = texthash 10
     `region`                            = texthash 10
     `postal_code`                       = texthash 10
   }
   `customers` = {
     `name`                              = texthash 5
     `description`                       = texthash 5
     `email`                             = emailhash 'magicmedia.studio' 10
     `tax_number`                        = fixed ''
     `website`                           = fixed ''
     `phone_number`                      = inthash 6
   }
   `legal_entities` = {
     `name`                              = texthash 5
     `usdc_wallet_address`               = texthash 10
     `vat_number`                        = texthash 5
   }
   `legal_entity_notification_settings` = {
     `notification_footer`                 = texthash 20
     `notification_contacts`               = fixed '["email@magicmedia.studio"]'
   }
   `users` = {
     `first_name`                        = texthash 5
     `last_name`                         = texthash 5
     `email`                             = emailhash 'magicmedia.studio' 10
     `password`                          = fixed '$2y$10$MpOqsrq07yMiJPlR2envRukCk2b9.tD9x9Dnv.pg97eqQ6Ha0Axli'
   }
}
