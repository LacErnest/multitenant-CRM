apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Values.app.name }}
  name: nginx-svc
  namespace: {{ .Values.app.name }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: app
