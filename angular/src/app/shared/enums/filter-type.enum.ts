export const enum FilterType {
  STRING = 'string',
  UUID = 'uuid',
  ENUM = 'enum',
  DECIMAL = 'decimal',
  INTEGER = 'integer',
  DATE = 'date',
  OBJECT = 'object',
  PERCENTAGE = 'percentage',
  BOOLEAN = 'boolean',
}
